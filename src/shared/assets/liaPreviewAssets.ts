import { finalRootAssets } from "./finalRootAssets";

/** Approved canonical artwork, byte-identical mirror in current-used/lia-preview. */
export const liaPreviewAssets = {
  avatar: finalRootAssets.lia.idleContemplative6f,
} as const;
