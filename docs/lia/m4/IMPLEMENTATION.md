# Mirador vivo

CONCEPT_A approved; final human score pending. CTA/title “Conversar con Lía” are FINAL_COPY under LIA-M4-01. The existing flag remains false by default. Mirador controls, route guards and M3 public contract are preserved. Conversation is a framed reading surface over the Mirador valley, with a substantial canonical Lía presence, secondary citations, bounded memory-only session, literal React text rendering and reduced motion.

The optional development laboratory requires both Vite DEV and VITE_LIA_TARGET_SIMULATION=true. Its module is dynamically imported and excluded from production. Explicit local fixture selection controls the sample response, not the typed question; this limitation is visible. CURRENT always uses the unchanged Core API. TARGET makes no conversation network request.

State mapping: entry_from_mirador and return_to_mirador use existing route/focus transition; presence_idle/greeting use canonical greeting first frame; listening is composer content; thinking is pending request; answering/explaining are supported replies; showing_sources is expanded details; clarifying/uncertain are abstention plus suggestion; safe_refusal and unavailable are explicit replies; farewell returns to Mirador; reduced_motion removes all decorative motion. Semantic states need not have separate new artwork. Generated files remain candidates outside runtime.
